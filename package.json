{
	"name": "template-worker-router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --target=esnext --platform=browser --loader:.html=text",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"dependencies": {
		"@spur.us/monocle-backend": "^0.5.1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.27",
		"@types/node": "^22.15.15",
		"esbuild": "^0.25.0",
		"vitest": "~3.1.0",
		"wrangler": "^4.14.3"
	}
}
